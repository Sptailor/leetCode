/**
 * @param {string[]} strs
 * @return {string[][]}
 */
var groupAnagrams = function(strs) {
    const map=new Map();
    const result=[];


    for(const str of strs){
        const sorted=str.split('').sort().join('');

    }

};